<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleMainActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello To Guitar Effect word"
        android:textColor="@color/colorWhite"
        android:textSize="30dp"
        app:layout_constraintBottom_toTopOf="@id/detailMainActivity"
        app:layout_constraintLeft_toLeftOf="parent"

        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:textColor="@color/colorWhitesoft"
        android:id="@+id/detailMainActivity"
        android:layout_width="match_parent"
        app:layout_constraintTop_toBottomOf="@+id/titleMainActivity"
        app:layout_constraintStart_toStartOf="@id/titleMainActivity"
        app:layout_constraintEnd_toEndOf="@id/titleMainActivity"
        android:layout_margin="20dp"
        app:layout_constraintBottom_toTopOf="@id/btnStart"
        android:text="Hi Welcome to my application. You can enjoy with this application. If you like my app, please comment this link >> http://"
        android:layout_height="wrap_content" />


    <Button
        android:id="@+id/btnStart"
        android:layout_width="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/detailMainActivity"
        app:layout_constraintStart_toStartOf="@id/detailMainActivity"
        app:layout_constraintEnd_toEndOf="@id/detailMainActivity"
        android:layout_marginTop="20dp"
        android:text="Start"
        android:layout_height="wrap_content" />

</androidx.constraintlayout.widget.ConstraintLayout>